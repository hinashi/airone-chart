apiVersion: apps/v1
kind: Deployment
metadata:
  name: gunicorn
  labels:
    {{- include "airone.labels" . | nindent 4 }}
    app.kubernetes.io/component: gunicorn
spec:
  replicas: {{ .Values.gunicorn.replicas }}
  selector:
    matchLabels:
      app: gunicorn
  template:
    metadata:
      labels:
        app: gunicorn
    spec:
      containers:
        - name: gunicorn
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
          {{- include "airone.env" . | nindent 12 }}
          command: ["/bin/sh"]
          args: ["-c", ". /config/dynamicenv.sh && /docker-entrypoint.sh gunicorn"]
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          configMap:
            name: airone-config
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
