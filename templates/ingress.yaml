apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: airone
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: airone
                port:
                  number: 8080
